# Security

Report suspected vulnerabilities privately to the repository owner rather than opening a public issue. Do not share WhatsApp session data, pairing codes, access tokens, or group metadata in logs or pull requests.

The bot should use least-privilege owner and administrator checks, validate all user-controlled input, avoid executing arbitrary code, and redact sensitive identifiers in operational logs.
